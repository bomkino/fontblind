#!/bin/sh
set -eu

if [ "$(id -u)" -eq 0 ]; then
  printf '%s\n' 'FontBlind refuses to run as root. Launch it from your normal KDE session.' >&2
  exit 77
fi

umask 077
export PYTHONDONTWRITEBYTECODE=1
exec /opt/fontblind/FontBlindServer/FontBlindServer --fontblind-browser-app "$@"
