# Security

Please do not disclose a vulnerability in a public issue. Use GitHub's private vulnerability-reporting flow for this repository.

FontBlind binds its worker to loopback, blocks external navigation, keeps runtime logs silent, and deletes ephemeral jobs. Reports that cross any of those boundaries are especially useful.
