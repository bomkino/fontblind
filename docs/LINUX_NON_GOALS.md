# Linux non-goals

This slice does not add a GTK shell, Electron, a daemon, package-manager repositories, automatic updates, telemetry, cloud storage, new font transformations, or a second runtime. It proves one honest Linux package around the existing FontBlind application.
