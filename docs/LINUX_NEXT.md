# After the Linux package foundation

Once the x86_64 package is green and manually checked, the next independent slices are:

1. aarch64 Linux package validation;
2. release automation from an exact tag, without auto-publishing;
3. desktop integration feedback and 3.6.x/3.7.x correctness fixes;
4. the larger Lab workflow: Inspect → Diagnose → Resolve → Build → Prove.

None of these expand the current pull request.
