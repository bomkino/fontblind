# Linux operator notes

Use `FONTBLIND_NO_OPEN=1` for headless validation. The launcher prints the exact loopback URL and stays attached to the frozen server. Terminating the launcher terminates the server and removes its private state directory.
