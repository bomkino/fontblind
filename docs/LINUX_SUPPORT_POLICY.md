# Linux support policy

The implementation recognises x86_64 and aarch64. Only architectures that complete the package job on a native CI runner are release-cleared. Unsupported distributions should first be reproduced against the portable bundle before changing the core runtime.
