# Linux local-only guarantees

The Linux package starts no daemon and binds no public interface. It launches one child process, opens one `127.0.0.1` URL, and keeps the child attached to the launcher lifecycle. Source bytes and generated outputs remain under the same anonymous-descriptor, retained-job, expiry, and sealed-download boundaries as the merged FontBlind 3.6 runtime.
